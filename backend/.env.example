# Copy this file to .env (or export these as real environment variables)
# and fill in real values locally. Never commit an actual .env file.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=smart_rental
DB_USERNAME=root
DB_PASSWORD=
